# API service only (Railway service name: "web"). Root Directory = repo root.
[build]
builder = "DOCKERFILE"
dockerfilePath = "sdr-api/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
