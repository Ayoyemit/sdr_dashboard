# API service (Railway name: "web") — Root Directory must be repo root.
[build]
builder = "DOCKERFILE"
dockerfilePath = "sdr-api/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
